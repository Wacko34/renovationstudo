{
  "scripts": {
    "pre-commit": "echo \"Lol\"",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.ts": "eslint --fix",
    "*.vue": "eslint --fix"
  },
  "workspaces": ["frontend"],
  "devDependencies": {
    "@commitlint/cli": "^18.2.0",
    "@commitlint/config-conventional": "^18.1.0",
    "husky": "^8.0.3",
    "lint-staged": "^14.0.0"
  }
}
